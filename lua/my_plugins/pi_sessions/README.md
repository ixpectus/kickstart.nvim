# pi_sessions

Telescope-пикер списка сессий Pi — id, количество сообщений, первый промпт, даты, длительность.

## Команды

`:lua require 'my_plugins.pi_sessions'.find()`

В пикере: Enter — открыть файл сессии, `y` — скопировать id, `m` — открыть в Markdown-формате.
