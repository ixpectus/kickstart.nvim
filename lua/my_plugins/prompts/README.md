# prompts

Telescope-пикер промптов из каталога `prompts/` — выбор, вставка, копирование.

## Команды

```vim
:PromptsList
```

`:lua require 'my_plugins.prompts'.list()`

В пикере: Enter — вставить промпт в буфер, `o` — открыть файл, `y` — скопировать содержимое.
