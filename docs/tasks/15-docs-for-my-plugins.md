Cформируй план и помести его в текущем файле в разделе #План 
ВАЖНО: не нужно писать план в чат, пиши его сразу в файл

Не начинай реализацию до того, как я одобрю план.
Если необходимо исследование сохраняй все артефакты в директории файла с задачей.

### Задача

Нужно добавить документацию для модуля exec /home/ixpectus/projects/kickstart.nvim/lua/my_plugins/exec/README.md
Она должна быть очень короткая и содержать команды вызова с примерами опций. Должны быть именно lua команды, которые можно выполнить в командном режиме для теста

---

### План

**Цель:** написать краткий `lua/my_plugins/exec/README.md` с командами для тестирования.

**Структура README.md:**

1. Заголовок `# exec`
2. Одно предложение — суть модуля (запуск файлов в scratch-окне)
3. Раздел `## Команды` с примерами Lua-кода для тестирования:
   - `:Exec /путь/к/файлу` — запустить файл
   - `:Exec` (без аргумента) — запустить текущий файл буфера
   - `:lua require 'my_plugins.exec'.run_file('/путь/к/файлу')` — вызов из Lua
4. Указать поддерживаемые типы файлов: `.lua` (через `lua5.4`), исполняемые shell-скрипты (по +x биту)
5. Ссылки на примеры файлов: `test/ls.sh`, `test/class.lua`

**Стиль:** максимально краткий, как другие README в `lua/my_plugins/` — без воды, только команды для копирования в терминал.

**Действия:**
1. Написать содержимое README.md
2. Записать в `lua/my_plugins/exec/README.md`
3. Запустить `stylua` на файл (если нужно)
4. Проверить через `lsp_diagnostics`

Реализацию начну после твоего одобрения.
