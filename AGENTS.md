## Rules

- Do not use `vim.schedule` for `vim.notify` without explicit need.
- Avoid unused variables and redundant assignments.
- Do not duplicate code — extract common logic into private functions.
- Check with lua-language-server after edits.
- Format with stylua.