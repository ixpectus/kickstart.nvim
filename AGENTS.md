## Правила

- Не использовать `vim.schedule` для `vim.notify` без явной необходимости.
- Избегать лишних присваиваний и переменных, которые не используются.
- Не дублировать код — выносить общую логику в приватные функции.
- проверять с помощью lua-language-server
- форматировать с помощью stylua
